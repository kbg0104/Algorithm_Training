#include <string>
#include <vector>

using namespace std;

string solution(vector<int> numbers) {
	string answer = "";
	vector<pair<int, int>> sss;
	pair<int, int> s;
	int i, j;
	for (i = 0; i < numbers.size(); i++) {
		s.second = numbers.at(i);
		if (numbers.at(i) / 10 == 0)
	}


	return answer;
}