#include <string>
#include <vector>
#include <iostream>
#include <sstream>

using namespace std;

string solution(string s) {
	string answer = "";
	vector<string> list;
	istringstream sum(s);
	int i;
	for (i = 0; i <= sum.size(); i++) {

	}
	for (i = 0; i <= list.size(); i++) {
		cout << list.at(i);
	}
	return answer;
}
void main() {
	int n;
	string s = "";
	cin >> s;
	solution(s);

}